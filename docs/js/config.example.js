// Scoop frontend config -- copy to config.js and fill in your keys.
// config.js is gitignored and never committed.

// Perplexity (for live demo on landing page)
window.SCOOP_PPLX_KEY = 'your-perplexity-key-here';

// Supabase (for signup RPC on landing page + auth on account page)
window.SCOOP_SUPABASE_URL = 'https://your-project.supabase.co';
window.SCOOP_SUPABASE_KEY = 'your-anon-key-here';       // for signup RPC
window.SCOOP_SUPABASE_ANON_KEY = 'your-anon-key-here';  // for Supabase Auth
